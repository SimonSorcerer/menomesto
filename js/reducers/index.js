const defaultState = {
    letter: 'A'
}

const reducers = (state = defaultState, action) => {
    switch (action.type) {
        case 'X':
        default:
            return state;
    }
}

export default reducers
